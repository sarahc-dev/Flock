import { render } from '@testing-library/react-native';
import { Text } from "react-native"
import NewEvent from '../../app/new-event';

// needs to be created
describe(NewEvent, () => {
//   test('renders the correct title', () => {
//     const choices = []
//     const { getByText } = render(<MatchResults choices={choices}/>);
//     const textElement = getByText('Your Matches');
//     expect(textElement).toBeDefined();
//   });

//   test('Scrollview contains the correct amount of text elements', () => {
//     const choices = ["GravyBoat", "SalmonHat"]
//     const { getByTestId } = render(<MatchResults choices={choices}/>);
//     const item1 = getByTestId("scroll-view-1");
//     const textElements = item1.findAllByType(Text);
//     expect(textElements.length).toBe(2);
//   })
});
